import { ShowIfAllValidDirective } from './show-if-all-valid.directive';

describe('ShowIfAllValidDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfAllValidDirective();
    expect(directive).toBeTruthy();
  });
});
